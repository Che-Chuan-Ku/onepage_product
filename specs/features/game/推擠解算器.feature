@ignore @command
Feature: 推擠解算器（共用元件）
  # 來源：需求 #38（推擠解算器）
  # 事件：StonesPushed, StoneRemovedOffBoard
  # 規則（Q3 已定）：連鎖推擠 —— 目的格有棋子則整列連推；末端棋子被推出棋盤外則移除；
  #   列中遇障礙物則整列不動。橫劈、縱劈、海浪三種效果共用同一套解算器。

  Rule: 後置（狀態）- 目的格有棋子時整列連鎖推擠

    Example: 推擠方向連續三格皆有棋子時全部往前推一格
      Given 一列棋子於 (7,5),(7,6),(7,7) 皆有子
      When 推擠解算器對該列施加方向 "RIGHT" 推力
      Then (7,5) 的子移至 (7,6)，(7,6) 的子移至 (7,7)，(7,7) 的子移至 (7,8)
      And 系統發布 StonesPushed 事件

  Rule: 後置（狀態）- 末端棋子被推出棋盤外時移除該子

    Example: 推擠使棋盤邊界上的棋子被推出棋盤
      Given (7,14) 有一顆棋子，推擠方向為 "RIGHT"（棋盤欄範圍 0..14）
      When 推擠解算器對該列施加推力
      Then (7,14) 的棋子被移除
      And 系統發布 StoneRemovedOffBoard 事件

  Rule: 前置（狀態）- 列中遇障礙物時整列不推擠

    Example: 推擠路徑上有障礙物時整列維持不動
      Given 一列棋子與方向路徑中包含一個障礙物格
      When 推擠解算器對該列施加推力
      Then 該列所有棋子維持原位
      And 不發布 StonesPushed 事件

  Rule: 後置（共用性）- 橫劈、縱劈、海浪推擠共用同一解算器行為

    Example: 海浪推擠與橫劈推擠遵循相同的連鎖與越界規則
      Given 海浪觸發對沙灘方向的推擠
      When 推擠解算器結算
      Then 其連鎖與越界移除規則與橫劈/縱劈完全一致

  # Q3 已定（A）：連鎖推擠；末端出界移除；遇障礙物整列不動；三效果共用解算器。
